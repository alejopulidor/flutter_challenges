import 'package:flutter/material.dart';
import 'package:flutter_challenges/boats/pages/home_page.dart';

class BoatsMain extends StatelessWidget {
  const BoatsMain({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return HomePage();
  }
}
